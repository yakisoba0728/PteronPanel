services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: pteron
      POSTGRES_PASSWORD: pteron
      POSTGRES_DB: pteron
    ports:
      - "5432:5432"
    volumes:
      - pteron_pgdata:/var/lib/postgresql/data

volumes:
  pteron_pgdata:
